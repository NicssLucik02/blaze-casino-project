export const enum InputTypes {
  TEXT = 'text',
  EMAIL = 'email',
  PASSWORD = 'password',
}

export const enum ButtonTypes {
  BUTTON = 'button',
  SUBMIT = 'submit',
  RESET = 'reset',
}

export enum AppRoutes {
  LOGIN = '/login',
  SIGNUP = '/signup',
  DASHBOARD = '/dashboard',
}
